post_install() {
	compat/linux/sbin/ldconfig -r /compat/linux/

	echo ">>> Please run:"
	echo "    nspluginwrapper -v -a -i"
	echo ">>> to install the plugin"

	echo "If using NVIDIA card and suffer blue tints,"
	echo "uncomment the first line in:"
	echo ">>> /compat/linux/etc/adobe/mms.cfg"
EOF
}
