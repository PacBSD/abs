# $Id: PKGBUILD 191784 2013-07-30 19:02:00Z jgc $
# Maintainer: Ionut Biru <ibiru@archlinux.org>

pkgname=gjs
pkgver=1.39.0
pkgrel=1
pkgdesc="Javascript Bindings for GNOME"
arch=(i686 x86_64)
url="http://live.gnome.org/Gjs"
license=(GPL)
depends=(cairo gobject-introspection 'js>=17.0.0')
options=('!libtool')
source=(http://ftp.gnome.org/pub/gnome/sources/$pkgname/${pkgver%.*}/$pkgname-$pkgver.tar.xz)
sha256sums=('d708d03d9ed5fe59acb1afa74cfa677f444bfe9a1eb7acda7dd31d9c71c0e05f')

build() {
  cd $pkgname-$pkgver
  ./configure --prefix=/usr --disable-static
  gmake
}

check() {
  cd $pkgname-$pkgver

  # Fails test. uuidgen invalid arguements:
  gmake check || true
}

package() {
  cd $pkgname-$pkgver
  gmake DESTDIR="$pkgdir" install
}
