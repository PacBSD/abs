# $Id: PKGBUILD 167697 2012-10-03 05:10:48Z eric $
# Maintainer: Anthony Donnelly <Amzo@PacBSD.org>

pkgname=libtiff
pkgver=4.0.5
pkgrel=2
pkgdesc="Library for manipulation of TIFF images"
arch=('amd64')
url="http://www.remotesensing.org/libtiff/"
license=('custom')
depends=('libjpeg' 'gcc-libs')
makedepends=('freeglut' 'glu' 'mesa' 'jbigkit')
optdepends=('freeglut: for using tiffgt')
source=(ftp://ftp.remotesensing.org/pub/libtiff/tiff-${pkgver}.tar.gz)
sha256sums=('e25eaa83ed7fab43ddd278b9b14d91a406a4b674cedc776adb95535f897f309c')

build() {
  cd "${srcdir}/tiff-${pkgver}"
  ./configure --prefix=/usr
  make
}

check() {
  cd "${srcdir}/tiff-${pkgver}"
  make check
}

package() {
  cd "${srcdir}/tiff-${pkgver}"
  make DESTDIR="${pkgdir}" install
  install -d -m755 "${pkgdir}/usr/share/licenses/${pkgname}/"
  install    -m644 COPYRIGHT "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
